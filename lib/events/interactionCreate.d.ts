import Client from '../classes/Client';
import { Interaction } from 'discord.js';
declare const _default: {
    new (): {
        run(client: Client, interaction: Interaction): Promise<void | any>;
        name: string;
    };
};
export = _default;
